// Minimal stubs for the bits of `youtipie` enum API that Namida touches.
// All YouTube features are considered disabled in this fork.

enum AccountCookiesValidity {
  valid,
  invalid,
  unknown,
}

