name: youtipie
description: "Local stub of youtipie for the Ahwfr/namida-ios fork (YouTube features disabled)."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

